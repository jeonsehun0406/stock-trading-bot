# 서버 무인 실행 가이드

`trading_bot.py`는 **한 번 실행되면 스캔을 1회만 하고 종료**하도록 만들어져 있습니다
(`main()` 안 주석: "여기선 예시로 1회. 스케줄러가 반복 호출"). 즉 봇 자체에는 무한루프가
없고, **외부 스케줄러(crontab 등)가 5~15분마다 반복 실행**해주는 걸 전제로 설계됐습니다.
그리고 봇 안에 중복주문 방지(`OrderGuard`, 같은 종목·액션은 하루 1회)가 있기 때문에
5분마다 실행돼도 실제 매매는 하루 최대 1회로 제한됩니다.

**이 문서만 보고 그대로 따라 하면 실제 서버 배포가 가능하도록** 명령어를 그대로 복붙할 수
있게 작성했습니다. 서버 환경(라즈베리파이/VPS)이 없는 상태에서 작성했기 때문에, crontab
문법·systemd 문법 자체는 검증했지만 **실제 서버에서의 동작은 사장님이 직접 확인해야
합니다** — 확인이 필요한 부분은 문장마다 명시했습니다.

---

## 1. 실행 환경 선택

| 방식 | 비용 | 전력/관리 | 안정성 | 비고 |
|---|---|---|---|---|
| 라즈베리파이 (집에 두고 24시간 운영) | 초기 5만~10만원, 이후 전기세만 | 저전력(몇 W), 정전·인터넷 끊김에 취약 | 중 (가정용 회선 의존) | 한 번 사면 끝, 다만 정전/공유기 재부팅에 봇도 같이 죽음 |
| 클라우드 VPS (AWS Lightsail, Oracle Cloud 무료티어, 네이버클라우드 등) | Oracle Cloud는 무료티어로 충분(Always Free), AWS Lightsail은 월 3.5~5달러부터 | 관리 거의 불필요, 데이터센터 전원·회선이라 안정적 | 상 | **추천.** 정전/인터넷 이슈를 신경 안 써도 됨 |

- 이 봇은 CPU·메모리 요구량이 거의 없는 스크립트(스캔 1회 실행 후 종료)라서, 가장 저사양
  무료/저가 인스턴스로도 충분합니다.
- **추천: Oracle Cloud Free Tier**(영구 무료 인스턴스 제공) 또는 이미 라즈베리파이가 집에
  있다면 그걸 그대로 사용. 둘 다 사장님이 아직 준비하지 않은 상태라면, 아래 "당장 로컬에서
  테스트" 섹션으로 먼저 흐름만 검증해보는 것을 권장합니다.

### 당장 서버가 없다면 — Windows 작업 스케줄러로 로컬 테스트 (테스트 전용, 무인 실행 아님)

⚠️ **이 방식은 PC를 꺼두면 봇도 같이 멈춥니다.** "무인 실행"이라는 원래 목표에는 맞지
않고, 어디까지나 **서버를 구하기 전에 크론 대신 흐름만 로컬에서 확인**해보는 용도입니다.

1. `run_bot.bat` 같은 배치 파일을 `stock_bot` 폴더에 만듭니다.
   ```bat
   @echo off
   cd /d "C:\Users\jshh0\ai\자동매매 프로그램\stock_bot"
   python trading_bot.py
   ```
2. Windows 검색 → "작업 스케줄러" 실행 → "작업 만들기"
   - 트리거: 매일, 반복 간격 5~15분, 기간 09:00~15:30 (한국장)
   - 동작: 위 `run_bot.bat` 실행
   - "컴퓨터가 절전 모드일 때 작업을 시작하지 않음" 옵션은 꺼두는 게 낫습니다(안 그러면
     화면이 꺼진 사이 실행이 안 됨).
3. PC가 켜져있고 로그인된 상태에서만 동작합니다. 실전 무인 운영은 아래 2번(Linux
   crontab)을 따르세요.

---

## 2. crontab 설정 (Linux, 메인 방법)

서버(라즈베리파이 OS, Ubuntu 등)에서 진행합니다.

### 2-1. 서버 시간대를 한국시간(KST)으로 맞추기

crontab은 서버의 로컬 시간대를 기준으로 동작합니다. VPS는 기본 UTC인 경우가 많으므로,
아래처럼 KST로 바꿔두면 이후 crontab 시간을 그대로 한국 시간 기준으로 적을 수 있습니다.

```bash
sudo timedatectl set-timezone Asia/Seoul
timedatectl   # Asia/Seoul (KST, +0900)로 나오는지 확인
```

(시간대를 UTC로 유지하고 싶다면 아래 crontab 시간에서 9시간을 빼서 입력하면 됩니다만,
헷갈리기 쉬우므로 위처럼 KST로 맞추는 걸 권장합니다.)

### 2-2. 프로젝트 준비

```bash
# 프로젝트 파일을 서버로 복사(scp/git 등) 후
cd ~/stock_bot
python3 -m venv venv
source venv/bin/activate
pip install python-kis python-dotenv pandas numpy matplotlib yfinance finance-datareader requests anthropic
cp .env.example .env
nano .env   # KIS 키, 텔레그램 등 입력. KIS_VIRTUAL=true 로 시작
python universe.py   # 유니버스 최초 1회 생성
```

### 2-3. 실행 래퍼 스크립트

crontab에서 바로 `python trading_bot.py`를 부르면 venv 활성화가 안 되고, 작업 디렉터리도
꼬일 수 있어 래퍼 스크립트를 하나 만드는 걸 권장합니다.

`~/stock_bot/run_bot.sh`:
```bash
#!/bin/bash
cd /home/USERNAME/stock_bot
source venv/bin/activate
python trading_bot.py >> logs/cron_stdout.log 2>&1
```

```bash
chmod +x ~/stock_bot/run_bot.sh
```

- `logs/bot_YYYY-MM-DD.log`는 봇이 `logging`으로 스스로 상세히 기록하는 메인 로그입니다
  (`trading_bot.py`의 `logging.FileHandler(f"logs/bot_{date.today()}.log", ...)`,
  188행 근처). 위 `cron_stdout.log`는 어디까지나 "크론 자체가 스크립트를 실행했는지,
  파이썬이 아예 뜨지도 못하고 죽었는지"를 확인하는 보조용 로그입니다. 평소 트러블슈팅은
  `logs/bot_*.log`를 우선 확인하세요.

### 2-4. crontab 등록

```bash
crontab -e
```

**한국장 (09:00~15:30 KST, 평일)** — 5분 간격:
```cron
*/5 9-14 * * 1-5 /home/USERNAME/stock_bot/run_bot.sh
0-30/5 15 * * 1-5 /home/USERNAME/stock_bot/run_bot.sh
```
(09:00부터 14:55까지 5분마다 + 15:00~15:30까지 5분마다 = 사실상 09:00~15:30 전체를
5분 간격으로 커버합니다.)

> ⚠️ **2026-09-01부로 실제 운영은 이 crontab 방식이 아니라 Railway(`stock-bot` 서비스)의
> 자체 크론 스케줄로 옮겼습니다.** GitHub Actions가 5분 간격처럼 자주 도는 스케줄을 몇
> 시간씩 밀리게 실행하는 걸 실제로 겪은 뒤(매수 신호를 "장종료"로 놓치는 사고 발생)
> Railway로 전환했습니다. 아래 crontab 섹션은 직접 서버(라즈베리파이/VPS)를 운영할
> 경우를 위해 남겨두지만, **Railway의 크론은 UTC 기준이라 아래 KST 시간을 그대로 못 쓰고
> 9시간을 빼서 변환**해야 합니다 (예: 서머타임 미국장 22:30~05:00 KST → `*/5 13-19 * * 1-5`
> UTC). 현재 Railway `stock-bot` 서비스의 `cronSchedule`은
> `*/5 0-6,11,13-19 * * 1-5` (한국장 + 저녁요약 + 미국장 서머타임 기준)로 설정돼 있고,
> 서머타임 전환 시기(3월/11월경)에 사람이 직접 Railway 대시보드나
> `railway api`로 이 값을 갱신해야 합니다(자동전환 없음, 아래 내용과 동일한 이유).

**미국장 — `.env`의 `ENABLE_US_TRADING=true`일 때만 필요.** 기본값(`false`)이면 이
블록은 등록하지 않아도 됩니다 (미국 스캔 자체를 안 하므로 실행해도 자원 낭비만 됩니다).

미국 정규장은 한국시간 기준으로 아래와 같이 걸쳐 있고, **서머타임 여부에 따라 1시간씩
당겨집니다.** 서머타임 자동전환 로직은 이번 범위에 넣지 않았으니, 대략 **3월~11월경(미국
서머타임 기간)은 아래 "서머타임" 블록을, 11월~3월경(표준시)은 "표준시" 블록을** 사람이
직접 바꿔가며 써야 합니다(사장님이 직접 날짜에 맞춰 crontab을 갈아끼워야 함 — 자동화
안 돼있음을 명시).

⚠️ 미국장은 자정을 넘어가므로, "새벽 부분"은 요일이 하루씩 밀립니다(미국 월요일 밤
세션이 한국시간으로는 화요일 새벽까지 이어짐). 아래 예시는 이를 반영했습니다.

- **표준시 (11월~3월경, 23:30~06:00 KST)**
  ```cron
  */10 23 * * 1-5   /home/USERNAME/stock_bot/run_bot.sh
  */10 0-5 * * 2-6  /home/USERNAME/stock_bot/run_bot.sh
  0 6 * * 2-6        /home/USERNAME/stock_bot/run_bot.sh
  ```
- **서머타임 (3월~11월경, 22:30~05:00 KST)**
  ```cron
  */10 22-23 * * 1-5 /home/USERNAME/stock_bot/run_bot.sh
  */10 0-4 * * 2-6   /home/USERNAME/stock_bot/run_bot.sh
  0 5 * * 2-6         /home/USERNAME/stock_bot/run_bot.sh
  ```
  (위 `22-23` 시간대에 22:00~22:25도 포함되지만, 아직 개장 전이라 봇이 실행돼도
  `KisMarketNotOpenedError`로 조용히 스킵됩니다 — 아래 6번 트러블슈팅 참고. 정확히
  22:30부터만 돌리고 싶다면 `30-59/10 22` 처럼 분 범위를 더 좁혀도 됩니다.)

**종가매매 — `.env`의 `ENABLE_CLOSING_TRADE=true`일 때만 필요.** 기본값(`false`)이면
등록하지 않아도 됩니다. 정규장(15:30) 마감 후 15:40~16:00 KST에만 장후시간외 주문이
접수되므로, 이 시간대에도 봇이 한 번 더 실행돼야 합니다:
```cron
*/5 15 * * 1-5  /home/USERNAME/stock_bot/run_bot.sh
```
(위 한국장 블록이 이미 15:00~15:30을 커버하므로, 15:30~16:00 구간만 겹치지 않게
추가하려면 `30-59/5 15 * * 1-5`처럼 분 범위를 좁혀도 됩니다. 15:40 이전에 실행돼도
`KisMarketNotOpenedError`나 "모의투자 미지원" 스킵으로 조용히 넘어가므로 크게 상관은
없습니다.) ⚠️ 이 기능은 모의투자(`KIS_VIRTUAL=true`)에서는 python-kis 라이브러리
단계에서부터 거부되어 항상 스킵되며, **실전투자로 전환하기 전까지는 검증 자체가
불가능합니다** — README 7단계 참고.

**확인 안 됨 / 사장님이 직접 테스트 필요**: 위 crontab 문법 자체(시간 범위·요일 표기)는
cron 표준 문법에 맞게 작성했지만, 실제 서버에 등록해 의도한 시각에 정확히 실행되는지는
반드시 `crontab -l`로 등록 확인 후 하루 지켜보며 검증해주세요.

---

## 3. 봇이 죽었을 때 자동 재시작

`trading_bot.py`의 `main()`은 이미 대부분의 예외를 잡아서 처리합니다(`서버_무인실행_가이드.md`
작성 시점 코드 기준, 899~931행):

- `TradingBot()` 생성 실패(예: `.env`에 `KIS_APP_KEY`가 없음) → `봇 시작 실패: {e}` 로그 +
  텔레그램/카톡 알림 후 정상 종료
- 계좌 조회 실패 → `계좌 조회 실패: {e}` 로그 후 정상 종료
- 스캔 도중 예외 → `봇 실행 중 치명적 오류` 로그(`log.exception`, 스택트레이스 포함) +
  `🚨 봇 다운!\n{예외타입}: {메시지}` 알림 후 정상 종료

즉 **거의 모든 경우, 프로세스는 "에러를 알리고 스스로 깔끔하게 종료"하지 "먹통 상태로
계속 떠있지"는 않습니다.** 이 상태에서는 별도의 재시작 장치가 없어도 다음 crontab
스케줄(최악 5~10분 뒤)이 다시 새 프로세스를 띄우면서 자연 복구됩니다.

다만 아래처럼 **파이썬 예외 처리로 못 잡는 경우**(서버 자체 재부팅, OOM으로 프로세스가
강제 종료(kill)됨, 정전 등)에는 crontab이 계속 재시도해주므로 이 역시 최악 5~10분
공백으로 자연 복구되지만, "서버가 아예 꺼진 채로 안 켜지는" 경우는 crontab도 무력합니다
— 이런 경우까지 막으려면 클라우드 VPS 제공업체의 "인스턴스 자동 재부팅" 설정이나
헬스체크 알림 서비스(예: UptimeRobot으로 봇 상태를 주기적으로 외부에서 확인)를 별도로
구성해야 하며, 이는 이번 문서 범위 밖입니다(필요하면 별도 작업으로 요청해주세요).

### systemd 서비스/타이머 방식 (선택 — crontab 대신 쓰고 싶은 경우)

crontab 대신 systemd timer를 쓰면 "직전 실행이 아직 안 끝났으면 겹쳐 실행하지 않는다"는
보장이 기본 제공되는 장점이 있습니다. (crontab은 이 보장이 없어서, 봇 실행이 5분보다
오래 걸리면 이론상 겹쳐 실행될 수 있습니다 — 다만 `OrderGuard`가 같은 종목 중복매수는
막아주므로 치명적이진 않습니다.)

`/etc/systemd/system/tradingbot.service`:
```ini
[Unit]
Description=Trading Bot (single scan)
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
WorkingDirectory=/home/USERNAME/stock_bot
ExecStart=/home/USERNAME/stock_bot/venv/bin/python /home/USERNAME/stock_bot/trading_bot.py
User=USERNAME
```

`/etc/systemd/system/tradingbot.timer`:
```ini
[Unit]
Description=Run Trading Bot every 5 minutes during market hours

[Timer]
# 아래는 예시입니다. OnCalendar는 systemd 캘린더 문법으로,
# 요일/시간 범위 지정이 cron보다 표현이 다릅니다 — 정교한 스케줄이 필요하면
# 여러 OnCalendar 라인을 겹쳐 등록하거나, 매 5분 실행 후 스크립트 내부에서
# 장 시간 여부는 어차피 KisMarketNotOpenedError로 스킵되므로 아래처럼 넓게 잡아도 됩니다.
OnCalendar=Mon..Fri *-*-* 09..15:00/5:00
Persistent=false

[Install]
WantedBy=timers.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now tradingbot.timer
systemctl list-timers | grep tradingbot   # 등록 확인
```

**확인 안 됨**: 위 `OnCalendar` 표현식은 systemd 캘린더 문법 규칙에 맞게 작성했지만
(`OnCalendar=Mon..Fri *-*-* 09..15:00/5:00` 형태), 15:30 정각에서 정확히 끊기지는
않고 15:55까지 5분 간격으로 도는 점은 crontab 예시와 다릅니다. 정밀하게 15:30에서
끊고 싶다면 crontab 방식을 권장하며, systemd를 쓴다면 `systemd-analyze calendar
"Mon..Fri *-*-* 09..15:00/5:00"` 명령으로 실제 다음 실행 시각들을 미리 서버에서
확인해보고 조정하세요.

---

## 4. 배포 전 체크리스트

- [ ] `.env` 파일 준비 완료 (`.env.example`을 복사해 작성)
  - [ ] `KIS_HTS_ID`, `KIS_ACCOUNT_NO`
  - [ ] `KIS_APP_KEY` / `KIS_APP_SECRET` (실전), `KIS_V_APP_KEY` / `KIS_V_APP_SECRET` (모의)
  - [ ] `KIS_VIRTUAL=true` — **반드시 모의투자로 시작**
  - [ ] `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` (또는 `KAKAO_REST_API_KEY`) — 알림 채널
  - [ ] `ANTHROPIC_API_KEY` — 없어도 동작은 하지만(fail-open), 뉴스 필터를 쓰려면 필요
  - [ ] `BOT_STRATEGY` — `compare_strategies.py`로 미리 비교해 정한 전략
- [ ] `python universe.py` 최초 1회 실행 → `universe.json` 생성 확인. **월 1회 재실행 권장**
  (봇이 스스로 재실행하지 않으므로 사람이 직접 재실행해야 함, `README.md` 3-1단계 참고)
- [ ] 모의투자(`KIS_VIRTUAL=true`)로 **최소 2주** 이상 crontab을 실제로 돌려보며 검증
  - 텔레그램/카톡 알림이 정상 도착하는지
  - `logs/bot_*.log`에서 매매가 의도대로 되는지
  - 서킷브레이커·중복주문방지가 실제로 작동하는지
- [ ] 검증이 끝난 뒤에만 `.env`의 `KIS_VIRTUAL=false`로 전환 + 소액부터 시작

---

## 5. 보안 체크리스트

⚠️ **`.env` 파일이 유출되면 KIS API 키가 그대로 노출되어 계좌가 위험해집니다.** 서버
배포 시 아래를 반드시 지켜주세요.

- [ ] **`.env` 파일 권한 제한**
  ```bash
  chmod 600 .env
  ```
  (소유자 본인만 읽고 쓸 수 있게 — 서버에 다른 사용자 계정이 있어도 못 읽음)

- [ ] **KIS API 키 발급 시 "주문 권한"만 부여, "출금 권한"은 빼기** — 정리노트에도 명시된
  원칙입니다. 키가 유출돼도 출금까지는 막을 수 있습니다.

- [ ] **SSH는 키 인증만 허용, 비밀번호 로그인 비활성화**
  ```bash
  sudo nano /etc/ssh/sshd_config
  # PasswordAuthentication no
  # PermitRootLogin no
  sudo systemctl restart sshd
  ```
  (SSH 키를 먼저 등록해 접속 가능한지 확인한 뒤에 비밀번호 로그인을 꺼야 서버에서
  잠기지 않습니다.)

- [ ] **방화벽 기본 정책 — 필요한 포트만 열기** (예: ufw 사용 시)
  ```bash
  sudo ufw default deny incoming
  sudo ufw default allow outgoing
  sudo ufw allow OpenSSH
  sudo ufw enable
  ```
  이 봇은 외부에서 접속받을 필요가 없는 스크립트이므로(전부 봇 → KIS 서버로 나가는
  아웃바운드 호출), 인바운드는 SSH 포트 외엔 열 필요가 없습니다.

- [ ] **`.gitignore` 확인** — 실제 파일 내용을 확인한 결과, 아래가 이미 포함돼 있습니다:
  `.env`, `*.env`, `kakao_token.json`, `kis_token*.json`, `bot_state.json`,
  `bot_state.json.tmp`, `logs/`, `*.log`(→ `logs/order_guard.json`,
  `logs/kelly_history.json`도 `logs/` 규칙으로 함께 보호됨)
  - `universe.json`은 **gitignore에 없습니다.** 다만 내용을 직접 확인한 결과 종목코드·
    종목명·시가총액·거래대금 등 **공개 시장 데이터만** 담고 있고 계좌/개인정보는 없어
    커밋해도 안전합니다(README에도 이 파일이 필요하다고 안내돼 있으므로 gitignore에
    넣지 않는 게 오히려 맞습니다).
  - 만약 git 저장소를 쓰지 않고 서버에 파일만 직접 복사(scp/rsync)하는 방식이라면
    `.gitignore`는 애초에 의미가 없으니, **서버 자체의 파일 권한(위 chmod 600)이
    실질적인 방어선**이라는 점을 기억하세요.

---

## 6. 로그 / 트러블슈팅

### 디스크 용량 관리

`logs/bot_YYYY-MM-DD.log`가 매일 새로 생성됩니다. 오래된 로그를 자동으로 정리하려면
crontab에 아래를 추가하세요 (30일 지난 로그 삭제, 매일 새벽 3시):

```cron
0 3 * * * find /home/USERNAME/stock_bot/logs -name "bot_*.log" -mtime +30 -delete
```

더 체계적으로 관리하고 싶다면 `logrotate`를 써도 됩니다
(`/etc/logrotate.d/tradingbot`에 `/home/USERNAME/stock_bot/logs/*.log` 대상으로
`daily`, `rotate 30`, `compress` 등을 설정).

### 자주 볼 수 있는 로그 메시지 (실제 코드에서 그대로 인용)

| 로그 문구 | 의미 | 조치 |
|---|---|---|
| `[005930 매수] 장이 열려있지 않습니다 — 스킵` (매도/시세 조회 시엔 `[005930 매도]`/`[005930 시세]`처럼 종목코드+동작으로 바뀝니다) | 정상. `KisMarketNotOpenedError` 발생 시 재시도 없이 즉시 스킵하는 안전장치(9번). 장중이 아닌 시간에 crontab이 실행됐을 때 흔히 보입니다 | 조치 불필요. 반복적으로 장중에도 뜬다면 서버 시간대(`timedatectl`)가 KST로 맞는지 확인 |
| `🚨 서킷브레이커 발동! 일일 손실 {N}% — 당일 매매 중단` | 정상 작동 중인 안전장치(2번). 하루 손실이 `-5%`(`trading_bot.py`의 `SETTINGS["daily_loss_limit"]`, 현재 `.env`로 조정 불가 — 코드 내 하드코딩) 이하로 떨어지면 당일 매매를 전면 중단 | 당일은 그대로 두면 됨(다음날 자동 리셋). 텔레그램/카톡으로도 즉시 통보됨 |
| `🇺🇸 USD 예수금 0원 — 미국 매수 스킵 (KIS 앱에서 환전 필요)` | `ENABLE_US_TRADING=true`인데 KIS 앱에서 원화→달러 환전을 안 해둔 경우 | KIS 앱(또는 홈페이지)에서 환전 후 대기. 봇은 에러 없이 조용히 스킵만 함 |
| `⚠️ [005930] 부분체결: 요청 10주 중 7주만 체결` | 정상 처리 중인 상황(8번 안전장치). 주문은 성공했지만 실제 체결 수량이 요청보다 적음. 대시보드·알림·`day_trades`엔 실제 체결량만 기록됨 | 조치 불필요, 자동 반영됨 |
| `🚨 [005930] 매도 실패 — 손절/익절 미실행, 수동 확인 필요 (...)` | **중요.** 재시도까지 전부 실패해 손절/익절이 실행되지 못한 상태(10번 안전장치) | 텔레그램/카톡으로도 즉시 통보됨. HTS나 MTS를 열어 수동으로 확인/매도 필요 |
| `[켈리] kelly_history.json 로드 실패 — 이력 없이 시작(균등분배로 폴백): ...` | `logs/kelly_history.json`이 손상됐거나 없음. 봇은 안전하게 균등분배로 폴백 | 이력이 사라진 것뿐, 봇은 정상 동작. 파일 손상이 반복되면 디스크 여유 공간 확인 |
| `봇 시작 실패: ...` | `.env` 설정 오류 등으로 `TradingBot()` 생성 자체가 실패 (예: `KIS_APP_KEY` 누락) | `.env` 값 재확인. 텔레그램/카톡으로도 알림 옴 |
| `계좌 조회 실패: ...` | KIS 서버 응답 이상 등으로 시작 시점 자산 조회 실패 | 다음 crontab 실행에서 자연 재시도됨. 반복되면 KIS 서버 상태/네트워크 확인 |
| `봇 실행 중 치명적 오류` (스택트레이스 포함) + `🚨 봇 다운!` 알림 | 스캔 도중 예상 못 한 예외 발생 | 로그의 스택트레이스로 원인 파악. 다음 crontab 스케줄에서 자연 재시도(3번 참고) |

---

## 참고 — 이 문서 작성 시 확인/미확인 구분

- **실제 코드와 대조해 확인함**: 로그 문구 전체(위 표), 파일 경로(`logs/bot_*.log`,
  `logs/order_guard.json`, `logs/kelly_history.json`), `.env` 변수명, `.gitignore`
  내용, `main()`의 예외 처리 흐름(스캔 1회 후 종료 구조).
- **cron/systemd 문법만 검증 가능, 실제 서버 동작은 미확인**: crontab 시간 표현식,
  systemd `OnCalendar` 표현식. 실제 서버에 배포한 뒤 `crontab -l` / `systemctl
  list-timers`로 등록을 확인하고, 최소 하루는 로그를 지켜보며 의도한 시각에 실행되는지
  검증해주세요.
- **이번 범위 밖으로 남긴 것**: 미국 서머타임 자동 전환(사람이 수동으로 crontab 교체),
  서버 자체가 완전히 꺼졌을 때의 복구(클라우드 VPS의 자동 재부팅 설정이나 외부
  헬스체크 서비스는 별도 작업으로 필요).
